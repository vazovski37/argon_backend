steps:
  # 1. Build the container image
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', 'europe-west3-docker.pkg.dev/$PROJECT_ID/argon-repo/argon-backend:$COMMIT_SHA', '.']
  
  # 2. Push the container image to Artifact Registry
  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', 'europe-west3-docker.pkg.dev/$PROJECT_ID/argon-repo/argon-backend:$COMMIT_SHA']
  
  # 3. Deploy container image to Cloud Run
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      - 'run'
      - 'deploy'
      - 'argon-backend'
      - '--image'
      - 'europe-west3-docker.pkg.dev/$PROJECT_ID/argon-repo/argon-backend:$COMMIT_SHA'
      - '--region'
      - 'europe-west3'
      - '--allow-unauthenticated'
      # We will set environment variables in the Cloud Run UI or via command line once, 
      # and they will persist across deployments.

images:
  - 'europe-west3-docker.pkg.dev/$PROJECT_ID/argon-repo/argon-backend:$COMMIT_SHA'

options:
  logging: CLOUD_LOGGING_ONLY
